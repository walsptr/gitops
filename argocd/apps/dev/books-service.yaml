apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: bookslib-dev-books-service
  namespace: argocd
spec:
  project: bookslib
  destination:
    server: https://kubernetes.default.svc
    namespace: bookslib-dev
  sources:
    - repoURL: REPLACE_ME_CHART_REPO_URL
      targetRevision: main
      path: helm/books-service
      helm:
        valueFiles:
          - $values/environments/dev/books-service.values.yaml
    - repoURL: REPLACE_ME_GITOPS_REPO_URL
      targetRevision: main
      ref: values
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
