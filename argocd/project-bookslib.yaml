apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: bookslib
  namespace: argocd
spec:
  description: BooksLib GitOps project
  sourceRepos:
    - "*"
  destinations:
    - server: https://kubernetes.default.svc
      namespace: "*"
  clusterResourceWhitelist:
    - group: "*"
      kind: "*"
