apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: vault
  namespace: bookslib-dev
spec:
  provider:
    vault:
      server: https://vault.syawal.local:8200
      path: kv
      version: v2
      auth:
        kubernetes:
          mountPath: kubernetes
          role: bookslib-dev-eso
          serviceAccountRef:
            name: bookslib-vault-auth
