apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: reviews-service-secrets
  namespace: bookslib-prod
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault
    kind: SecretStore
  target:
    name: reviews-service-secrets
    creationPolicy: Owner
  data:
    - secretKey: DB_PASSWORD
      remoteRef:
        key: bookslib/prod/reviews-service
        property: db_password
