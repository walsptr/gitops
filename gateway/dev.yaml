apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: auth-dev
  namespace: bookslib-dev
spec:
  parentRefs:
    - name: syawal-gateway
      namespace: nginx-gateway
      sectionName: http
  hostnames:
    - auth-dev.syawal.local
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: bookslib-dev-auth-service-auth-service
          port: 8081
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: books-dev
  namespace: bookslib-dev
spec:
  parentRefs:
    - name: syawal-gateway
      namespace: nginx-gateway
      sectionName: http
  hostnames:
    - books-dev.syawal.local
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: bookslib-dev-books-service-books-service
          port: 8080
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: reviews-dev
  namespace: bookslib-dev
spec:
  parentRefs:
    - name: syawal-gateway
      namespace: nginx-gateway
      sectionName: http
  hostnames:
    - reviews-dev.syawal.local
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: bookslib-dev-reviews-service-reviews-service
          port: 8000
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: bookslib-dev
  namespace: bookslib-dev
spec:
  parentRefs:
    - name: syawal-gateway
      namespace: nginx-gateway
      sectionName: http
  hostnames:
    - bookslib-dev.syawal.local
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: bookslib-dev-frontend-frontend
          port: 80

